[build]
  command = "npm run build"
  publish = "dist"

# Beställningsformuläret (src/components/OrderForm.astro) renderas som ren
# statisk HTML med data-netlify="true", vilket räcker för att Netlify ska
# hitta och registrera det vid bygget — inget särskilt formulär-plugin
# behövs. Ställ in mottagaradress för formulärnotiser i Netlifys
# adminpanel (Site settings → Forms → Notifications) mot
# cafe.contact.orderEmail i src/data/cafe.ts när den är bekräftad.
