[build]
  command = "npm run build"
  publish = "dist"

# Beställningssektionen (src/components/OrderForm.astro) innehåller TVÅ
# statiska Netlify-formulär, båda renderade som ren HTML med
# data-netlify="true" — inget särskilt formulär-plugin behövs:
#   - "bestallning" (src/components/OrderShop.astro) — vardagssortimentet
#   - "specialbestallning" (src/components/SpecialRequest.astro) — tårta,
#     moussetårta och annat utan fast pris
# Ställ in mottagaradress för formulärnotiser för BÅDA i Netlifys
# adminpanel (Site settings → Forms → Notifications) mot
# cafe.contact.orderEmail i src/data/cafe.ts när den är bekräftad.
